import { styled } from "@mui/system";
import { FullContainerWithSideBar } from "src/styles";

export const InvitesContainer = styled(FullContainerWithSideBar)({
  paddingLeft: 20,
  display: "flex",
  flexDirection: "column",
  gap: 15,
  height: "100%",
});
